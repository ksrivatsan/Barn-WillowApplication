%h1 Listing fabrics

%table
  %thead
    %tr
      %th Type
      %th Color
      %th Image url
      %th
      %th
      %th

  %tbody
    - @fabrics.each do |fabric|
      %tr
        %td= fabric.type
        %td= fabric.color
        %td= fabric.image_url
        %td= link_to 'Show', fabric
        %td= link_to 'Edit', edit_fabric_path(fabric)
        %td= link_to 'Destroy', fabric, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Fabric', new_fabric_path
